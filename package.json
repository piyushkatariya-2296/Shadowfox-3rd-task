{
  "name": "pulsefit-studio",
  "version": "1.0.0",
  "description": "Full-stack web application for PulseFit Studio - modern fitness and wellness platform",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client"
  },
  "keywords": ["fullstack", "react", "express", "mongodb", "tailwind", "portfolio"],
  "author": "PulseFit Development Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
